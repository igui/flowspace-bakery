version: '3'
services:
  mydb:
    image: postgres
    environment: 
      POSTGRES_USER: bake
      POSTGRES_PASSWORD: somecookies
    volumes:
    - db-data:/var/lib/postgresql/data
    ports:
    - 5432:5432
volumes:
  db-data:
    driver: local